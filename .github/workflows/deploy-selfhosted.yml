name: Deploy to Self-Hosted Server

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-selfhosted
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  API_IMAGE: ghcr.io/bratan-corp/bratan-music-api
  WEB_IMAGE: ghcr.io/bratan-corp/bratan-music-web

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    name: Build & Push Images
    runs-on: ubuntu-latest
    outputs:
      api_digest: ${{ steps.api.outputs.digest }}
      web_digest: ${{ steps.web.outputs.digest }}

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ── API image ──
      - name: Build & push API image
        id: api
        uses: docker/build-push-action@v6
        with:
          context: ./worker
          file: ./worker/Dockerfile
          push: true
          tags: |
            ${{ env.API_IMAGE }}:latest
            ${{ env.API_IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ── Frontend (nginx + built SPA) ──
      - name: Build & push Web image
        id: web
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.web
          push: true
          tags: |
            ${{ env.WEB_IMAGE }}:latest
            ${{ env.WEB_IMAGE }}:${{ github.sha }}
          build-args: |
            VITE_API_URL=https://bratan-music.eu.cc/api
            VITE_BASE=/
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - uses: actions/checkout@v4

      # ── Install cloudflared for SSH access via CF Tunnel ──
      - name: Install cloudflared
        run: |
          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o /tmp/cloudflared.deb
          sudo dpkg -i /tmp/cloudflared.deb

      # ── Set up SSH with cloudflared proxy ──
      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          cat >> ~/.ssh/config << EOF
          Host deploy-server
            HostName ${{ secrets.DEPLOY_HOST }}
            User ${{ secrets.DEPLOY_USER }}
            IdentityFile ~/.ssh/deploy_key
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
            ProxyCommand cloudflared access ssh --hostname %h
          EOF

      # ── Deploy: pull new images & restart ──
      - name: Deploy to server
        run: |
          ssh deploy-server << 'DEPLOY'
            set -e
            cd /opt/bratan-music

            # Update repo (for config changes)
            cd app && git fetch origin && git reset --hard origin/main && cd ..

            # Sync deploy configs from repo
            cp app/deploy/docker-compose.yml deploy/docker-compose.yml
            cp -r app/deploy/nginx deploy/

            # Pull new images and restart
            cd deploy
            docker compose pull api nginx
            docker compose up -d --remove-orphans

            # Clean up old images
            docker image prune -f

            # Health check
            sleep 10
            STATUS=$(curl -sf http://localhost/api/health | grep -c '"ok"' || true)
            if [ "$STATUS" -eq 1 ]; then
              echo "✅ Deploy successful — API healthy"
            else
              echo "❌ API health check failed!"
              docker compose logs api --tail 20
              exit 1
            fi
          DEPLOY

  # ── First-time server setup (manual trigger only) ──
  setup:
    name: Initial Server Setup
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4

      - name: Install cloudflared
        run: |
          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o /tmp/cloudflared.deb
          sudo dpkg -i /tmp/cloudflared.deb

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          cat >> ~/.ssh/config << EOF
          Host deploy-server
            HostName ${{ secrets.DEPLOY_HOST }}
            User ${{ secrets.DEPLOY_USER }}
            IdentityFile ~/.ssh/deploy_key
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
            ProxyCommand cloudflared access ssh --hostname %h
          EOF

      - name: Setup server
        run: |
          ssh deploy-server << 'SETUP'
            set -e

            # Install Docker if missing
            if ! command -v docker &>/dev/null; then
              echo "Installing Docker..."
              curl -fsSL https://get.docker.com | sh
              systemctl enable docker && systemctl start docker
            fi

            if ! docker compose version &>/dev/null; then
              apt-get update -qq && apt-get install -y docker-compose-plugin
            fi

            echo "Docker: $(docker --version)"
            echo "Compose: $(docker compose version)"

            # Clone repo if not present
            mkdir -p /opt/bratan-music
            if [ ! -d /opt/bratan-music/app/.git ]; then
              git clone --depth 1 -b main https://github.com/BRATAN-CORP/bratan-music.git /opt/bratan-music/app
            else
              cd /opt/bratan-music/app && git fetch origin && git reset --hard origin/main
            fi

            # Copy deploy configs
            mkdir -p /opt/bratan-music/deploy/nginx
            cp /opt/bratan-music/app/deploy/docker-compose.yml /opt/bratan-music/deploy/docker-compose.yml
            cp -r /opt/bratan-music/app/deploy/nginx/conf.d /opt/bratan-music/deploy/nginx/conf.d

            # Generate .env if not present
            if [ ! -f /opt/bratan-music/deploy/.env ]; then
              echo "⚠️  No .env file found! Create /opt/bratan-music/deploy/.env with required secrets."
              exit 1
            fi

            # Login to GHCR
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Pull and start
            cd /opt/bratan-music/deploy
            docker compose pull
            docker compose up -d

            echo "✅ Initial setup complete!"
          SETUP
