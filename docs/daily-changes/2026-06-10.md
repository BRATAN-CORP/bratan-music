# 2026-06-10 — Глобальный рефакторинг + фиксы плеера (PR refactor/global-cleanup-and-player-fixes)

Один PR, атомарные коммиты. Полный план/отчёт: [[refactor/global-refactor-2026-06]].

## Рефакторинг (поведение не менялось)
- `temp/todo.md` удалён (мёртвый скретчпад Go-порта).
- `API_BASE`: 7 локальных копий → единый экспорт из `src/lib/api.ts`.
- `toPlayable` → общий `toPlayerTrack` в `src/lib/playerTrack.ts`.
- album/artist like-хуки в `useLibrary.ts` → одна фабрика `makeLibraryItemLikeHooks` (track-хуки осознанно отдельно).
- eslint warnings 3 → 0 (`load` → `useCallback` в админ-панелях).

## Фиксы
- **fix(stream)**: корень «макс. качество не играет» — сегментированный DASH (SegmentTemplate без `<BaseURL>`) теперь помечается непроигрываемым в `api-go/internal/tidal/stream.go`, ступень пропускается на сервере за один round-trip. Парность в worker/TidalWeb.ts. Тест `TestDecodeManifestDASHTemplateIsUnplayable`.
- **fix(player)**: фолбек качества — hard media error больше не ретраится (до ~46 с на ступень), ретрай только для таймаутов (×1).
- **fix(player,ios)**: `navigator.audioSession.type='playback'`; probe записываемости `audio.volume` — iPhone её игнорирует, поэтому mute и глушение слотов теперь через `audio.muted`; graph-less кроссфейд на iPhone деградирует в чистый hard cut вместо двойного звука.
- **chore(deploy)**: бек целиком на Go — legacy TS `api`-контейнер удалён из compose и деплой-workflow. `worker/` остаётся (init-db + контракт парности), удаление отдельным PR.

## Не тронуто
- `useAudioPlayer` не резался на модули (движок защищён) — см. трекер.
- CI (`ci.yml`) ещё гоняет worker lint/typecheck — убрать вместе с удалением `worker/`.
