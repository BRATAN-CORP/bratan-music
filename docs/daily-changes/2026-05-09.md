# 2026-05-09

## Bug bash: 50-track daily playlists, /auth UX, linked accounts, logged-out request flood — `devin/1778329012-bug-bash-50tracks-auth-linked`

**Когда:** 2026-05-09 12:20 UTC
**Кто:** Devin (по запросу пользователя)
**Branch:** `devin/1778329012-bug-bash-50tracks-auth-linked`
**PR:** см. PR в репозитории

### Что изменилось

**1. Daily playlists — гарантия 50 треков.**
- `worker/src/services/DailyPlaylistService.ts`:
  - В `generateVariants()` добавлен бэкфилл: если после фильтра по
    дизлайкам в варианте остаётся `< PLAYLIST_LENGTH` треков, перебираем
    `spec.fallbackGenres → genreSeeds → ['genre_pop','genre_rap','genre_electronic']`,
    подмешивая через `mergeUnique` до 50.
  - В `buildVariantTracks()` для варианта `discover` убран gate
    `hasHistory` для filler — теперь genre-seeded подсыпка работает и
    для пользователей с историей, у которых wave вернул мало unknown
    треков.
  - Добавлен жёсткий cap `tracks.slice(0, PLAYLIST_LENGTH)` после
    бэкфилла, чтобы не отдать > 50 в БД (UI на главной рассчитывает на
    ровно 50).

**2. Profile «Связанные аккаунты» — редизайн.**
- `src/components/features/LinkedAccountsPanel.tsx`:
  - Снят внутренний `border bg-background/40` со строк Telegram/Email —
    теперь это плоский `divide-y` список внутри общего
    `bg-card`/`border-border` контейнера, как у других карточек на
    профиле (`SettingsCard`).
  - У бейджа «Привязан» убран `border-[var(--color-accent)]/30`,
    который в тёмной теме читался как белая обводка (dark accent =
    светло-сиреневый). Остался прозрачный `bg-[var(--color-accent-soft)]`
    + `text-[var(--color-accent)]`.
  - Удалён дублирующий `<Lock />` иконкой при привязанной почте —
    статус «Привязан» уже передаёт badge.
  - Иконка email вынесена в `EmailGlyph()` (9×9, в стиле
    `TelegramGlyph`), бейдж — в `StatusBadge()`.
- `src/i18n/locales/ru.json`, `src/i18n/locales/en.json`: удалён
  неиспользуемый `profile.linkedAccounts.emailLockedHint`.

**3. /auth/email — порядок Telegram → divider → email + унификация фона.**
- `src/components/features/EmailLoginCard.tsx`: удалён внутренний
  `── или по почте ──` divider (он раньше всегда висел над формой и в
  отрыве от Telegram-кнопки выглядел инвертированно).
- `src/app/auth/email/page.tsx`:
  - Перевёрстан flow: Telegram-кнопка → divider `или по почте` →
    `<EmailLoginCard />`. Так структура соответствует читаемой логике
    «основной способ — Telegram, альтернатива — почта».
  - Карточка теперь `bg-card` (без `/80 backdrop-blur-sm`), как другие
    карточки в сервисе.
  - Убран `altLoginHint` / `altLoginCta` блок снизу (back-кнопка сверху
    и Telegram-кнопка сверху уже покрывают навигацию).
- `src/i18n/locales/{ru,en}.json`: удалены неиспользуемые
  `auth.emailPage.altLoginHint`, `auth.emailPage.altLoginCta`,
  обновлены `eyebrow` и `subtitle`.

**4. /auth карточка — фон.**
- См. выше: `bg-card/80 backdrop-blur-sm` → `bg-card`. Теперь визуально
  совпадает с `SettingsCard` / `LinkedAccountsPanel` / другими
  карточками на профиле.

**5. Бесконечные запросы на `/library/playlists`, `/library/likes/ids`
после logout / без логина.**
- `src/hooks/useLibrary.ts`:
  - Добавлен helper `useIsAuthed()` (читает `accessToken !== null`).
  - На все read-side хуки (`usePlaylistsList`, `useLikedTracks`,
    `useLikedIds`, `useLikedAlbumIds`, `useLikedArtistIds`,
    `useLikedAlbums`, `useLikedArtists`) повешен `enabled: isAuthed`.
- Корень проблемы: `Sidebar`, `Player`, `MobileBottomDock`,
  `FullscreenPlayer` смонтированы на каждом маршруте и тянули эти
  query-хуки. Без `enabled`-gate они стреляли в Worker сразу при
  логауте/без логина → Worker отвечал 429 → React-Query с
  `retry: 1` повторял каждый дважды, и в network-вкладке это
  выглядело как «бесконечная пальба».
- `useDislikesQuery` уже имел `enabled: isAuthed` — взят как образец.

### Почему

Все пять багов из чата пользователя:
1. «Иногда генерится не 50, а 10/30 треков в плейлистах дня».
2. «У бейджа «привязан» странная белая обводка в тёмной теме» +
   карточка «Связанные аккаунты» визуально вырывается из ряда
   профильных карточек, иконка замка дублирует статус.
3. «На /auth/email порядок компонентов перепутан — сначала надпись
   «или по почте», потом форма почты, без Telegram сверху».
4. «Карточка /auth не затемнена как другие карточки в сервисе».
5. «После логаута / без логина Worker заваливается запросами на
   `/library/playlists` и `/library/likes/ids`».

### Документация в `docs/`

- Этот файл (`docs/daily-changes/2026-05-09.md`).
- `docs/structure/frontend/` и `docs/structure/worker/` карты не
  расширялись новыми сущностями — всё в существующих файлах. Если
  следующий агент будет переписывать `LinkedAccountsPanel` или
  `DailyPlaylistService`, имена/контракты файлов сохранены, ссылки на
  них из карт не сломаны.

### Что важно знать следующему агенту

- `useIsAuthed()` сейчас приватный хелпер внутри `useLibrary.ts`. Если
  понадобится в других хуках — вынести в `src/store/auth.ts` или в
  отдельный `src/hooks/useIsAuthed.ts`. Сейчас просто следует
  существующему паттерну `useDislikesQuery`.
- `PLAYLIST_LENGTH = 50` в worker — единственная константа размера
  плейлиста дня. UI (`src/app/home/page.tsx` + др.) отображает первые
  10 треков плюс остальное в детальной странице, и предполагает
  ровно 50.
- `EmailLoginCard` теперь не рисует свой собственный divider; это
  ответственность вызывающей страницы. На сегодня единственный
  потребитель — `src/app/auth/email/page.tsx`.
- Если ещё какие-то query-хуки бьют по защищённым endpoint'ам без
  `enabled: isAuthed` (особенно в `useLibrary.ts` и `useExplore`-
  подобных) — повесить gate по тому же образцу.

---

## Hero kebab + OTP auto-submit + link Telegram — `devin/1778330021-hero-kebab-otp-autosubmit`

**Когда:** 2026-05-09 12:55 UTC
**Кто:** Devin (по запросу пользователя)
**Branch:** `devin/1778330021-hero-kebab-otp-autosubmit`

### Что изменилось

**1. Hero action rail сворачивается в кебаб на узких экранах.**
- Новый компонент `src/components/features/HeroActionsKebab.tsx` —
  лёгкая обёртка над `PopoverMenu` (тот же портал, что и у
  `TrackKebabMenu`), триггер — `IconButton` с `MoreHorizontal`. Caller
  сам передаёт `MenuItem`/`MenuDivider` детей, поэтому контракт
  максимально совместим с существующим паттерном.
- `src/app/artist/page.tsx`: вторичные действия (Ban/Unban, Radio,
  Share) теперь в `<div className="hidden sm:contents">` (видны от
  `sm:` 640 px и выше) + `<HeroActionsKebab className="sm:hidden">`
  ниже sm. Хендлеры `handleToggleArtistDislike` / `handleShareArtist`
  вынесены над JSX, чтобы inline-кнопки и пункты меню звали один и тот
  же код.
- `src/app/album/page.tsx`: то же самое для Share (offline-кнопка
  оставлена inline — она единственная, и на узких экранах сама по себе
  не переносит ряд).
- `src/app/playlist/page.tsx` остался без изменений: его иконки
  (rename / share / pin / offline) уже встроены в строку заголовка
  `flex flex-wrap`, отдельного action rail нет.
- Логика «поделиться» вынесена из `ShareButton` в helper
  `src/lib/share.ts` (`buildAppShareUrl`, `shareLink`) — оба сёрфейса
  (inline `ShareButton` + `MenuItem` в кебабе) зовут один и тот же
  код и не дублируют Web Share / clipboard / `prompt()` chain.

**2. Авто-сабмит OTP-кода после 6-го символа.**
- `src/components/features/EmailLoginCard.tsx`: вынесен
  `submitCode(value)` из `handleVerify`. Добавлен `useEffect`, который
  на 6 цифрах в `code` (и `step === 'code'`, `!submitting`) вызывает
  `submitCode(code)` напрямую. После серверной ошибки `submitCode`
  очищает `code` в `''`, что предотвращает мгновенный повторный
  авто-сабмит.
- `src/components/features/LinkedAccountsPanel.tsx` (in-profile email
  link form) пока без авто-сабмита — отдельный сёрфейс, фокус был на
  unauth-странице по запросу пользователя; легко добавить тем же
  паттерном при необходимости.

**3. Привязка Telegram у email-first аккаунтов.**
- Миграция `worker/src/db/migrations/0027_users_tg_id.sql`:
  - `users.tg_id` (TEXT, UNIQUE) — для email-first юзеров стартует с
    NULL, выставляется при привязке. Для tg-first строк back-fill
    `tg_id := id`, чтобы новый lookup `findByTgId` находил их так же,
    как старый `findById`.
  - `tg_link_requests` (nonce / requester_id / tg_id / tg_username /
    tg_name / expires_at / created_at) — короткоживущая таблица для
    bot-mediated nonce flow. TTL = 5 минут, GC висит на cron-сwееpе.
- `worker/src/services/UserService.ts`:
  - `findByTgId(tgId)` — lookup по `tg_id`, fallback на `id`.
  - `upsert()` теперь ищет существующую строку через `findByTgId()` —
    email-first user после link-flow резолвится в свой `email_…` row,
    а не плодит новую tg-строку (которую UNIQUE на `tg_id` всё равно
    отверг бы).
  - `linkTelegram(userId, tg)` — выставляет `tg_id`/`tg_username`/
    `tg_name`. Бросает `tg_id_taken`, если этот tg_id занят другим
    юзером.
- `worker/src/routes/auth.ts`: `isNew` теперь считается через
  `findByTgId()`, чтобы привязанный email-юзер не попадал под per-IP
  signup gate.
- `worker/src/routes/user.ts`:
  - `POST /user/me/telegram/link/start` — минтит nonce, кладёт строку
    в `tg_link_requests` с `requester_id = userId`. Возвращает nonce
    + `expiresAt`.
  - `GET /user/me/telegram/link/status/:nonce` — статус-поллер;
    `pending` / `expired` / `confirmed` / `conflict`. На `confirmed`
    зовёт `linkTelegram` и удаляет nonce-row (one-shot).
- `worker/src/bot/commands/start.ts`: добавлен ветка `args[0] ===
  'link_<nonce>'` — обновляет `tg_link_requests` с tg-данными
  отправителя и НЕ зовёт `ensureUser` (минимизирует риск UNIQUE
  conflict при последующем merge).
- `worker/src/cron.ts`: GC-сweep `DELETE FROM tg_link_requests WHERE
  expires_at < ?` рядом с `auth_nonces`-сweep'ом.
- `worker/src/routes/admin.ts`: при `DELETE /users/:id/data`
  чистится также `tg_link_requests WHERE requester_id = ?` (нет
  CASCADE FK).
- `src/components/features/LinkedAccountsPanel.tsx`: на ряде Telegram
  для email-first пользователя (`!hasTelegram && profile?.email`)
  показывается кнопка «Привязать Telegram». По клику зовёт
  `/user/me/telegram/link/start`, открывает
  `t.me/<bot>?start=link_<nonce>` в новой вкладке и поллит
  `/status/:nonce` каждые 1.5 c. На `confirmed` — `patchUser` +
  refetch профиля + toast. На `expired`/`conflict` — соответствующий
  toast и сброс состояния. Hard-stop через 5 минут.
- i18n: `profile.linkedAccounts.telegramLinkCta`,
  `telegramAwaiting`, `telegramLinkedToast`, `telegramExpired`,
  `telegramConflict`, `telegramStartFailed`. Добавлен `hero.moreActions`.

### Почему

- Hero на артисте/альбоме/плейлисте на узких viewport переносил
  Radio/Share/Ban на вторую строку, что читалось как «сломанная
  вёрстка». Кебаб с тем же шаблоном, что и у трека, унифицирует UX.
- OTP-форма требовала клика по «Подтвердить» после ввода 6 цифр —
  лишнее действие на мобиле, где кнопка к тому же мелкая.
- Email-first пользователи не имели способа добавить Telegram к
  аккаунту — пришлось бы регистрироваться заново через TG-flow и
  получать дубликат-аккаунт с потерянной библиотекой.

### Что важно знать следующему агенту

- `HeroActionsKebab` намеренно тонкий — это просто `PopoverMenu` с
  предзаданным trigger'ом. Если захочется добавить новые pages с
  hero-rail (room? episode?), переиспользуй компонент тем же
  паттерном (`hidden sm:contents` для inline + `sm:hidden` для
  кебаба).
- `lib/share.ts/shareLink()` принимает `path` (например `/album/123`)
  и сам префиксит origin/base. Для трек-share уже есть
  `buildTrackShareUrl` + inline-логика в `TrackKebabMenu`/
  `ShareButton`; не дублируй, а зови этот хелпер.
- `submitCode()` в `EmailLoginCard` — async, но `useEffect`
  возвращает sync-функцию, поэтому используем `void submitCode(code)`.
  `eslint-disable react-hooks/exhaustive-deps` стоит намеренно: deps
  ограничены до `[code, step]`, потому что включение `submitCode` в
  массив re-fire'ило бы effect на каждом keystroke (функция
  пересоздаётся каждый рендер).
- Миграция 0027 ОБЯЗАТЕЛЬНО должна примениться до раскатки бекенда —
  `findByTgId()` упадёт без колонки `tg_id`. На staging выполняй
  `wrangler d1 execute … --file=worker/src/db/migrations/0027_users_tg_id.sql`
  до деплоя воркера.
- Деплой бота тоже обязателен: `start.ts` должен уметь обрабатывать
  `link_<nonce>` deeplink, иначе ссылка попадёт в обычный fallback
  (помощь-меню) и nonce-row никогда не получит tg-данные.

---

## Player every-other-track stutter (corsRetried symmetry fix only)

**Когда:** 2026-05-09 13:05 UTC (изменено 2026-05-09 14:50 UTC после реверта визуальных изменений)
**Кто:** Devin (по запросу пользователя)
**Branch:** `devin/1778331271-player-crossfade-strobe` (визуальная часть откачена в `devin/1778333137-revert-fullscreen-fx`)

### Что изменилось

**Чёт-нечётный обрыв кроссфейда (1→2 ок, 2→3 рвётся, 3→4 ок, 4→5 рвётся).**
- `src/hooks/useAudioPlayer.ts`:
  - `startCrossfade` non-preloaded path (после успешного
    `tryLoadSrc`): теперь выставляется `corsRetried[incoming] = true`.
  - `startCrossfade` preloaded promotion path: то же —
    `corsRetried[incoming] = true` сразу после промоушена.
  - Preload effect (фоновый префетч `queue[idx+1]`): после
    успешного `tryLoadSrc` — `corsRetried[incoming] = true`.
- Корень: `loadTrack` (только initial-track path) выставляет
  `corsRetried[slot] = true` после успешной загрузки, чтобы
  потенциальный mid-stream `error` в дальнейшем НЕ дёргал
  `reloadWithoutCors()` (который зовёт `audio.load()` и
  принудительно сбрасывает `currentTime` в 0 + ставит на паузу).
  Все остальные пути загрузки (preload + промоушен через
  кроссфейд) этот флаг НЕ выставляли. Из-за этого слот A после
  стартового `loadTrack(t1)` стабильно имел `corsRetried[a] = true`,
  а слот B (никогда не проходивший через `loadTrack`) — постоянно
  `corsRetried[b] = false`. На любую транзиентную ошибку
  (range-request 4xx, тайм-аут, плавающий CORS-заголовок Tidal-
  CDN) на слоте B вызывался `reloadWithoutCors`, который
  обрывал ramp/паузил аудио, что воспринималось как «пауза, без
  плавного перехода, иногда стопает».
- Слот A был активен на нечётных треках (1, 3, 5, …), слот B —
  на чётных (2, 4, 6, …). Поэтому пользователь видел паттерн:
  «1→2 ок, 2→3 ломается, 3→4 ок, 4→5 ломается».

### Что отменено (относительно ранее зафиксированных правок)

- **Bass-driven white strobe** на блюр-фоне fullscreen-плеера
  (PR #422, FullscreenPlayer.tsx) — пользователь попросил
  откатить.
- **Cover-tinted strobe** + slow drift на блюр-фоне (PR #423) —
  откачено вместе с визуалом из #422.
- **`useBassPulse(active, resetKey?)` + `ctx.resume` в RAF tick**
  (PR #423, useAudioPlayer.ts) — откачено, сигнатура хука
  возвращена к оригинальной `useBassPulse(active)`. Существующий
  halo-эффект на TiltCard продолжает работать как раньше.

### Почему откатили

Пользовательский запрос: «сделай реверт до момента как было в
PR #421, только оставь момент фикса, связанный с every-other-track
stutter on auto-crossfade». То есть оставить только функциональный
fix кроссфейда, а все визуальные эксперименты в fullscreen-плеере
вернуть к состоянию #421.

### Что важно знать следующему агенту

- `corsRetried` — это «бюджет на одну CORS-перезагрузку слота
  на src». После любого успешного `tryLoadSrc` бюджет должен
  гасить КАЖДЫЙ путь, который этот src туда положил. Сейчас
  в коде четыре таких пути: (a) `loadTrack`, (b) preload effect,
  (c) `startCrossfade` non-preloaded, (d) `startCrossfade`
  preloaded promotion. Если появится пятый — обязательно
  ставить `corsRetried[slot] = true` сразу после успеха.
- FullscreenPlayer возвращён к состоянию #421: блюр-кавер на
  static image, halo с pulse+flash от `useBassPulse`, тёмный
  градиент 15/30/50, никаких дополнительных слоёв.


## Offline lyrics — `devin/1778362098-offline-lyrics`

**Когда:** 2026-05-09 21:30 UTC
**Кто:** Devin (по запросу пользователя)
**Branch:** `devin/1778362098-offline-lyrics`
**PR:** см. PR в репозитории

### Что изменилось

- `src/lib/offline/types.ts`:
  - Новый интерфейс `OfflineLyrics` (зеркалит `LyricsResponse` от
    `/tracks/:id/lyrics` плюс `fetchedAt`).
  - В `OfflineTrack` добавлено опциональное поле `lyrics: OfflineLyrics`.
- `src/lib/offline/streamResolver.ts`: добавлена функция
  `fetchLyricsPayload(trackId, source)` — best-effort fetch лирики,
  никогда не бросает (ошибки → null), пропускает upload-треки.
- `src/lib/offline/downloads.ts :: runTrack`: лирика запрашивается
  параллельно с `fetchAudioBlob` (нулевая прибавка к wall-clock),
  записывается в IDB одной транзакцией с аудио-блобом. Никогда не
  затирает существующую офлайн-лирику пустым ответом.
- `src/lib/offline/index.ts`: экспортируем `fetchLyricsPayload` и
  тип `OfflineLyrics` через публичный API модуля.
- `src/hooks/useLyrics.ts`: новая стратегия — IDB-first, потом сеть,
  на сетевой ошибке re-check IDB; при успешной сетевой загрузке
  back-fill офлайн-строки если у неё ещё нет лирики (для треков,
  скачанных до этой фичи).

### Почему

Пункт 1 батча 2026-05-09: «при загрузке трека в офлайн режим
прогружать его lyrics, чтобы они работали и в офлайн режиме (PWA
точно чтоб работало)». До этого PR `useLyrics` всегда ходил в
`/tracks/:id/lyrics`, и при отсутствии сети офлайн-плеер показывал
spinner навсегда.

### Документация в `docs/`

- [[structure/frontend/index]] — обновил описание `useLyrics` (теперь
  IDB-aware с back-fill).
- [[refactor/tracker]] — новый roadmap батча 4 задач, started status
  на этом PR, handoff log.

### Что важно знать следующему агенту

- IDB-схема НЕ менялась — `OfflineTrack.lyrics` опциональный, IDB
  принимает старые строки без поля. `useLyrics` относится к
  отсутствию `lyrics` как к «надо сходить в сеть и запатчить».
- `fetchLyricsPayload` всегда возвращает `Promise<… | null>`, никогда
  не throws — поэтому он безопасно стартует параллельно с аудио и не
  может уронить весь download. Auth-ошибки на лирике игнорируются
  (download продолжается).
- При unsave-track лирика автоматически уйдёт вместе со строкой
  трека (это просто поле). Никакой отдельной cleanup-логики не нужно.

---

## 2026-05-09 ~22:30 UTC — батч из 4 фиксов: volume slider, solid skip icons, PWA navbar inset, mini-player touch hit area

**Кто:** Devin (по запросу пользователя — "все остальные оставшиеся фиксы делай батчем")
**Branch:** `devin/1778363267-batch-fixes`
**PR:** #427

### Что изменилось

- `src/components/layout/FullscreenPlayer.tsx`:
  - Volume slider fill — убран класс `transition-[width] duration-100`.
    Полоса теперь обновляется в том же кадре что `pointermove` event
    (как в мини-плеере). Старая 100мс CSS-анимация читалась как лаг
    при перетаскивании.
  - SkipBack / SkipForward (lines 1310, 1318) — добавлены
    `fill="currentColor" strokeWidth={0}` для solid-варианта lucide
    глифа.

- `src/components/layout/Player.tsx`:
  - SkipBack / SkipForward (lines 448, 456) — solid prop'ы.

- `src/components/layout/MobileBottomDock.tsx`:
  - SkipForward (line 335) — solid prop'ы (SkipBack здесь нет —
    в моб. доке только Play/Pause/Next).
  - Bottom inset (line 151): формула `bottom` изменена с
    `calc(var(--pwa-safe-bottom)+1rem)` на
    `calc(var(--pwa-safe-bottom)/2+1rem)`. На iOS PWA с home
    indicator (~34px) док больше не задирается на ~17px выше
    его боковых отступов. Вне PWA переменная всё ещё `0px` →
    итоговое `1rem` (16px) совпадает с side padding.
  - Progress rail wrapper переделан: было
    `<div className="h-[3px] ..." onPointerDown={...}>` (3px одновременно
    визуал + hit area), стало
    `<div className="h-3.5 flex items-start ..." onPointerDown={...}>
       <div className="h-[3px] ...">{fill}</div>
     </div>`. Hit zone теперь 14px, видимая полоса 3px top-aligned.
    11px пустого пространства снизу съели только `py-2.5` cover row,
    интерактивные элементы в строке не пострадали.

### Почему

- **Volume slider:** мини-плеер уже был без transition, в фуллскрине
  оставалась 100мс CSS-анимация → дёрганый drag.
- **Solid skip icons:** у пользователя ощущение что lucide outline
  глифы выглядят инородно рядом с solid Play/Pause. Дефолтная
  заливка `none` + `strokeWidth=2` → клин-контур, после фикса
  `fill="currentColor"` + `strokeWidth=0` → solid wedge.
- **PWA navbar:** `safe-area-inset-bottom` на iOS включает home
  indicator (~34px), поэтому bottom-вариант с `+1rem` давал ~50px
  отступ — сильно больше чем 16px по бокам. Деление на 2 даёт
  ~33px (всё ещё > home indicator), визуально близко к side gap.
- **Mini-player touch:** жалоба "через мышку в DevTools работает,
  через touch emulator нет". Палец имеет физический радиус ~20px,
  попасть в 3px полосу почти невозможно. Видимая полоса осталась
  3px (визуал не меняется), hit zone = 14px (`h-3.5`).

### Документация в `docs/`

- `docs/refactor/tracker.md` — таблица roadmap'а обновлена
  (PR #425/426 merged, PR #427 open для батча, online/offline
  toast — открытая задача).

### Что важно знать следующему агенту

- **Online/offline toast (#6) НЕ ВКЛЮЧЁН в #427** — пользователь
  жаловался что не работает, но я воспроизвёл локально (npm run
  dev → DevTools console → `window.dispatchEvent(new
  Event('offline'))` → toast "You're offline" появляется;
  `dispatchEvent(new Event('online'))` → "Back online"). Логика в
  `OfflineToastWatcher.tsx` корректна (lastOnline ref защищает
  от phantom toast'ов на смене локали, useOnline честно слушает
  window 'online'/'offline' events). Если пользователь видит
  проблему только в установленной PWA — вероятная причина
  устаревший SW. Стоит проверить:
  - SW регистрация в `src/main.tsx` / `vite.config.ts` PWA plugin.
  - `<AppUpdateWatcher />` показывает ли prompt на новую версию.
  - В установленной PWA: Settings → Storage → Clear для теста.
- **Audio engine не трогался** — все правки только в JSX (skip
  icons, volume slider styling, navbar position, progress rail
  hit-area). useAudioPlayer / Howl / EQ нетронуты.
- **`MobileBottomDock` импортирует только SkipForward** —
  мини-док был дизайнером сделан без previous-кнопки (в фуллскрине
  и десктопном плеере она есть).
- **Hit-area на мини-плеер progress bar теперь 14px** —
  если в будущем понадобится сделать ещё больше (для accessibility),
  можно увеличить до `h-4` (16px) или `h-5` (20px). Cover row уже
  имеет `py-2.5` (10px top padding), так что увеличение hit-area
  до 13px (3+10) визуально безопасно. Дальше 13px начнут страдать
  cover image.
